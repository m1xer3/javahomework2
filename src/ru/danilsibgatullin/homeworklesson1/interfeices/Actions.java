package ru.danilsibgatullin.homeworklesson1.interfeices;

public interface Actions {
     boolean run(double distance);
     boolean jump(double distance);
     String whoAreYou();
     String getName();
}
